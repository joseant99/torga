# ===================================================================
# Spring Cloud Config bootstrap configuration for the "aws" profile
# ===================================================================

spring:
    application:
        name: torgaPedidos
    cloud:
        config:
            fail-fast: true

cloud:
    aws:
        stack:
            name: torgaPedidos
            auto: false
